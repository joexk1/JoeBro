# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.moved-aside
.swiftpm/

# Python
__pycache__/
*.pyc

# Local runtime data (never commit a user's data/keys)
*.sqlite3
.env
.env.*

# Dev artifacts (not for release)
test_screenshots/

# Release build outputs — DMGs go to GitHub Releases, not the repo.
# (appcast.xml IS tracked: it's the Sparkle update feed.)
dist/
.build-dd/
.sparkle-tools/
